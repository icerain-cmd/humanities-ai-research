# DeerFlow 일일 기술 분석: 2026-07-13

## 키틀러의 하드웨어 결정론과 생성형 AI — 분포 결정론을 향한 조건부 전환

### 1. 문제의 소재: 키틀러의 하드웨어 결정론과 생성형 AI의 충돌

프리드리히 키틀러(Friedrich Kittler)의 하드웨어 결정론은 "매체가 메시지를 결정한다"는 맥루언의 명제를 물질적 극한까지 밀어붙인다. 키틀러에게 중요한 것은 소프트웨어의 겉모습이 아니라, 전기 신호를 처리하는 트랜지스터, 디스크의 자기 패턴, 광섬유의 광자 — 즉 말 그대로의 하드웨어였다. 그의 유명한 선언 "소프트웨어는 존재하지 않는다"는, 모든 소프트웨어 층위는 결국 하드웨어의 물리적 상태(전압, 클록 사이클, 논리 게이트)로 환원될 수 있다는 급진적 유물론을 함축한다.

그러나 생성형 AI 시대는 이 명제를 근본적으로 흔든다. GPT-4의 1.8조 개 파라미터가 분포하는 트랜지스터 게이트는, 그 자체로는 메시지를 결정하지 않는다. 출력을 결정하는 것은 훈련 데이터의 통계적 분포와 추론 시점의 프롬프트 임베딩이라는 전적으로 소프트웨어적 조건이기 때문이다.

### 2. 생성형 AI의 세 가지 결정 층위

생성형 대규모 언어 모델(LLM)에서 출력의 결정은 단일한 인과 사슬이 아니라, **세 가지 층위가 중첩된 조건부 체계**로 작동한다.

#### (가) 모델 가중치 — 동결된 훈련 분포의 결정

모델 가중치는 특정 시점에 동결된(frozen) 훈련 데이터의 통계적 규칙성을 저장한다. 이는 키틀러의 "하드웨어"와 유사한 고정성(fixity)을 지닌다. 한 번 훈련된 가중치는 변경되지 않으며, 모델이 접근할 수 있는 지식의 범위와 패턴을 근본적으로 제한한다. 이 층위는 **동기적 결정(synchronic determination)**이라 부를 수 있다. 즉 훈련이 완료된 순간, 출력의 가능성 공간(likelihood space)은 이미 닫혀 있다.

Khoj 참고문헌 문서 3의 개념을 빌리면, 이것은 **아카이브(Archive)의 원리** — 안정성, 고정성, 보존성 — 에 해당한다. 가중치라는 아카이브는 한번 저장된 통계적 분포를 변경하지 않는다는 전제 위에 구축된다.

그러나 여기서 키틀러적 결정론과의 결정적 차이가 발생한다: 하드웨어는 물리적 법칙에 의해 출력이 결정되지만, 가중치는 **통계적 확률**에 의해 출력을 결정한다. 동일한 입력에 대해 온도 매개변수에 따라 전혀 다른 출력이 생성될 수 있다. 이는 하드웨어 결정론이 전제하는 결정적 인과성(deterministic causation)과 양립할 수 없는 특성이다.

#### (나) 데이터 분포 — 역사적 조건의 결정

모델이 훈련된 데이터의 분포는 더 근본적인 결정 층위다. 이 층위에서 중요한 것은:
- **무엇이 포함되었는가**: 인터넷 텍스트, 학술 논문, 코드 저장소
- **무엇이 배제되었는가**: 언어적·문화적 소수성, 비디지털화된 지식
- **어떻게 가중치가 부여되었는가**: Reddit 링크 구조, Wikipedia 편집 빈도, Common Crawl의 도메인 분포

이 층위는 **통시적 결정(diachronic determination)**으로, 훈련 데이터가 수집·선별·정제된 역사적 과정 자체가 출력의 조건이 된다. Khoj 참고문헌 문서 1이 지적하듯, "공정성은 단순한 기술적 변수의 조정이 아니라, 철학적 정의관과 문화적 세계관, 나아가 정치적 선택이 응축된 결과"다. 데이터 분포에는 이미 이러한 선택들이 각인되어 있다.

#### (다) 프롬프트 임베딩 — 현재적 조건의 결정

추론 시점의 프롬프트는 가장 즉각적인 결정 층위다. 동일한 모델, 동일한 가중치, 동일한 데이터 분포에서도 프롬프트의 미묘한 차이(문구, 맥락, 예시, 역할 부여)가 전혀 다른 출력을 생성한다. Khoj 문서 3이 지적하는 **매트릭스(Matrix)의 원리** — 유동성, 맥락 의존성, 매번 다른 출력의 생성 가능성 — 가 여기서 작동한다.

프롬프트 임베딩은 결정론적 인과성을 넘어, **확률적 조건화(stochastic conditioning)**의 영역에 속한다. 온도 매개변수(T)가 0에 가까울수록 결정론적에 가까워지고, 높을수록 창발성(emergence)의 여지가 커진다.

### 3. 재구성: 하드웨어 결정론 → 분포 결정론

Khoj 문서 5의 제목이 명시적으로 제시하듯, 키틀러식 하드웨어 결정론은 생성형 AI 시대에 **분포 결정론(distribution determinism)**으로 재구성되어야 한다. 이 재구성은 단순한 개념 대체가 아니라 다음 세 가지 축에서의 조건부 변환을 의미한다.

#### 축 1: 물질성 → 통계성

| 키틀러의 하드웨어 결정론 | 생성형 AI의 분포 결정론 |
|-------------------------|------------------------|
| 물리적 인과성 (전압·게이트) | 통계적 확률 (로그-확률 분포) |
| 결정적 출력 | 확률적 샘플링 |
| 환원 가능 (모든 층위는 하드웨어로) | 비환원적 층위 (가중치·데이터·프롬프트 각각 독립적 결정력을 가짐) |
| 동기적 (회로 설계 시점에 결정 완료) | 통시적+동기적 복합 (훈련 분포의 역사 + 추론 조건의 현재) |

#### 축 2: 단일 결정 → 조건부 중층 결정

키틀러에게 결정은 단일했다. 칩의 논리 게이트가 물리적 법칙에 따라 입력을 출력으로 변환한다. 생성형 AI에서는 결정이 **조건부로 중층화(conditional stratification)**된다:

```
데이터 분포의 결정 (훈련 역사)
        ↓
모델 가중치의 결정 (동결된 분포)
        ↓
프롬프트 임베딩의 결정 (추론 조건)
        ↓
온도 매개변수 (확률적 변이)
        ↓
최종 출력
```

각 층위는 하위 층위의 조건 위에 **추가적인 제약과 가능성의 공간**을 부과한다. 이는 키틀러의 수직적 환원(모든 것을 하드웨어로)이 아니라, **수평적 중층화(horizontal stratification)**다.

#### 축 3: 소프트웨어의 부정 → 소프트웨어의 물질적 재정의

키틀러가 "소프트웨어는 존재하지 않는다"고 선언했을 때, 그는 소프트웨어가 하드웨어 상태의 착시일 뿐이라고 주장했다. 그러나 생성형 AI는 이 명제를 역전시킨다: **소프트웨어(가중치·임베딩·분포)가 하드웨어(트랜지스터)를 특정한 방식으로 작동하게 만든다.** 동일한 GPU 위에서 동일한 전력과 클록으로 작동하는 모델이라도, 로드된 가중치에 따라 전혀 다른 출력을 생성한다.

이것은 키틀러의 물질적 극단주의가 간과한 **정보의 물질성(materiality of information)**의 다른 국면을 드러낸다. 하드웨어의 결정력이 사라진 것이 아니라, **결정의 장소(locus)**가 하드웨어의 논리 게이트에서 데이터 분포의 통계적 구조로 이동했다.

### 4. Eco-Digital 프레임워크로의 확장

이 재구성은 Eco-Digital 프레임워크와 연결될 때 더욱 풍부해진다.

**인공 자연(Artificial Nature)의 4요소 체계와의 접점**: 데이터 분포는 인공 자연의 데이터베이스 층위에, 모델 가중치는 알고리즘 층위에, 프롬프트 임베딩은 UI/UX 층위에 대응한다. 키틀러의 결정론이 무시한 것은 바로 이 인터페이스 층위의 결정력이다.

**공동체의 감각(Community of Sense) 4층 구조**: L1(물질 기반)은 하드웨어(GPU, 클러스터), L2(알고리즘 층위)는 데이터 분포와 모델 가중치, L3(인터페이스 층위)는 프롬프트 엔지니어링과 사용자 상호작용, L4(공명적 깊이)는 생성적 출력이 형성하는 의미의 장(場) — 이 구조에서 키틀러의 결정론은 L1에만 국한되었으나, 생성형 AI는 L2-L3-L4의 복합적 결정을 요구한다.

**WE 개념과의 연결**: 분포 결정론이 의미하는 것은 개별 주체(사용자/프로그래머)의 전적인 통제 불가능성이다. 훈련 데이터의 역사적 편향, 모델의 통계적 특성, 프롬프트의 예측 불가능한 효과는 키틀러가 말한 "인간의 매체에 대한 예속"의 새로운 형태 — 그러나 이번에는 하드웨어가 아니라 분포(distribution)에 대한 예속 — 를 형성한다. 여기서 WE 개념은 이 분포 결정론에 대한 조건부적 대응으로 기능할 수 있다.

### 5. 결론: 조건부 전환의 필요성

키틀러의 하드웨어 결정론은 생성형 AI 시대에 단순히 유효하지도, 무효하지도 않다. 그것은 **조건부로 전환(conditional turn)**되어야 한다:

- **유효한 측면**: 물질적 인프라(GPU, 데이터센터, 전력망)가 여전히 결정적 역할을 한다. AI의 물질적 비용(전력, 물, 희토류)은 키틀러가 강조한 매체의 물질성을 다시 한번 전면에 드러낸다.
- **무효한 측면**: 출력의 결정적 원인으로서 단일 하드웨어 인과성을 주장할 수 없다. 통계적 확률, 데이터 분포, 프롬프트 조건화라는 비물질적이면서도 물질적 효과를 지닌 층위들이 결정의 중추가 되었다.
- **재구성된 명제**: "매체가 메시지를 결정한다"는 키틀러적 명제는 "**분포가 출력의 가능성 공간을 결정한다**"로 재구성되어야 한다. '분포'는 훈련 데이터 분포, 가중치 분포, 임베딩 공간에서의 확률 분포를 모두 포괄하며, 이 분포들의 물질적 조건(훈련에 사용된 GPU, 데이터의 저장 매체, 추론이 실행되는 하드웨어)은 여전히 키틀러적 물질성의 영역에 속한다.

### 참조
- DeerFlow: 2026-07-13 21:00 KST
- Khoj RAG 문서 5: "키틀러의 하드웨어 결정론과 생성형 AI — 분포 결정론을 향한 조건부 전환"
- Khoj RAG 문서 3: "생성형 AI 시대의 서사학과 기호학: 기호학적 전환을 위한 시론" (아카이브-매트릭스 구분)
- Khoj RAG 문서 1: "제2부 1장. 공정한 검색은 없다 - 알고리즘 편향의 세계" (알고리즘 편향의 정치성)